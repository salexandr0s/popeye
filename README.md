# Popeye

Popeye is a local-first agent runtime built on a controlled Pi fork.

This repository now contains the initial monorepo scaffold, security policy foundations, and phase 1-6 implementation building blocks described in `architecture.md` and `buildplan.md`.
