# @popeye/cap-medical

Medical vault capability for Popeye. This package owns `medical.db`, a restricted SQLite store for medical imports, appointments, medications, documents, and periodic digests. All medical data is classified as `restricted` sensitivity with `none` embedding and context-release policies.
