# @popeye/cap-finance

Finance capability for Popeye. Owns `finance.db` as a restricted vault database for transaction imports, document tracking, full-text search over transactions, and periodic digest generation.
